import React from "react";
import styles from "./PrettyButton.module.css";

const PrettyButton = ({ text }) => {
  return <button className={styles.PrettyButton}>text</button>;
};

export default PrettyButton;
